define([
  'vb/action/actionChain',
  'vb/action/actions',
  'vb/action/actionUtils',
], (
  ActionChain,
  Actions,
  ActionUtils
) => {
  'use strict';

  class extendRequestActionChain extends ActionChain {

    /**
     * @param {Object} context
     * @param {Object} params
     * @param {object} params.event
     * @param {any} params.originalEvent
     */
    async run(context, { event, originalEvent }) {
      const { $page, $flow, $application, $constants, $variables } = context;

      const extendRequestDialogOpen = await Actions.callComponentMethod(context, {
        selector: '#extendRequestDialog',
        method: 'open',
      });
    }
  }

  return extendRequestActionChain;
});
